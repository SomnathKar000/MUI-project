import React from "react";
import AllProducts from "./AllProducts";

import { Box } from "@mui/material";

const Feed = () => {
  return (
    <Box flex={4}>
      <AllProducts />
    </Box>
  );
};

export default Feed;
